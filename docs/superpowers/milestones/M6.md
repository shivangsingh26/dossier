# M6 — Watchlist + Company Intel (Scoped Stub)

> **Status:** Stub. Say "expand M6".

**Spec:** [docs/superpowers/specs/2026-05-18-dossier-saas-frontend-design.md](../specs/2026-05-18-dossier-saas-frontend-design.md) §7 (tier matrix: Pro adds these), §10 (`/watchlist`).

**Goal:** Unlock Pro tier. `/watchlist` page shows watchlist-sourced jobs separately. Company intel modal on each JobCard. Tier-gated `RunButton` for watchlist (8 credits) + company-intel (3 credits/company).

## Files (frontend)

- `frontend/app/(app)/watchlist/page.tsx`
- `frontend/components/dossier/watchlist/WatchlistTable.tsx` (companies tracked + last seen + match count)
- `frontend/components/dossier/jobs/CompanyIntelModal.tsx`
- `frontend/components/dossier/TierGate.tsx` (overlay for Lite users)

## Files (backend)

- New agents in pipeline worker: `watchlist`, `company_intel` (already in SDK — just dispatch from worker)
- `GET /jobs/{id}/intel` returns cached intel.md
- Update `POST /pipeline/run` to accept agents=["watchlist"] and agents=["company_intel"]

## Acceptance criteria

- Pro user clicks "Run watchlist" → 8 credits debited → SSE progress → jobs appear in /watchlist
- Each JobCard shows "🏢 View intel" → modal opens with cached intel
- Lite user sees TierGate overlay on watchlist + intel — clicking "Upgrade" creates waitlist row
- `pytest backend/tests/test_watchlist.py -v` green
