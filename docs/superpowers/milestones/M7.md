# M7 — Gap Analysis Page (Scoped Stub)

> **Status:** Stub. Say "expand M7".

**Spec:** [docs/superpowers/specs/2026-05-18-dossier-saas-frontend-design.md](../specs/2026-05-18-dossier-saas-frontend-design.md) §10 (`/gaps`), §7 (Pro tier).

**Goal:** `/gaps` page — Pro feature. Visualize skill frequency in seen JDs vs user's profile. Bar chart, sortable, with "your skill / market wants" diff.

## Files (frontend)

- `frontend/app/(app)/gaps/page.tsx`
- `frontend/components/dossier/gaps/SkillFrequencyChart.tsx` (recharts or @nivo/bar)
- `frontend/components/dossier/gaps/GapTable.tsx` (skills + market%, profile match, gap severity)

## Files (backend)

- `backend/src/dossier_api/routers/gaps.py` — `GET /gaps`, `POST /pipeline/run agents=[gap_analysis]`
- Wire `dossier_sdk.agents.gap_analysis` into worker

## Acceptance criteria

- Pro user clicks "Run gap analysis" → 4 credits → results stored
- `/gaps` renders chart sorted by market frequency desc
- Each row shows: skill name · market % · ✓/✗ in profile · gap severity
- Lite user sees upgrade overlay
- Color: skills user has = mint, skills missing = peach, partial = warm
- `pytest backend/tests/test_gaps.py -v` green
